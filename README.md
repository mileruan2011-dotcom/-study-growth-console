# Growth Console

Local-first study tracker.

## Android Chrome PWA install note

After deploying a new version with PWA support, remove any old Android home-screen shortcut first. Then open the deployed site in Chrome and use **Install app** or **Add to Home screen** again so Android Chrome reads the updated manifest and launches Growth Console in standalone mode without the browser address bar.
